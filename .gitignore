# Build outputs
dist
.vercel/output

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.output
.vinxi
.tanstack/**
.nitro
*.local

# Environment — never commit real values. See .env.example for the required names.
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.verify/


# The repository's lockfile is bun.lock. package-lock.json is only produced by
# incidental `npm install` runs (Vercel uses `npm install`, which does not need it).
package-lock.json
