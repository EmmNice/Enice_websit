# Memory Index

- [Git push from agent shell is broken](git-push-broken.md) — `git push origin main` from the main agent's bash tool always hangs/times out; user must push via Replit's own Git panel instead.
- [Vercel + bun install unreliable](vercel-bun-install.md) — bun install on Vercel intermittently fails with connection errors; switched Vercel install/build commands to npm while keeping bun for local dev.
