/**
 * Single source of truth for the public site URL.
 * Ships to both client and server — no secrets here.
 *
 * To change the domain, update this one constant.
 * All route meta tags, canonical links, og:url, and JSON-LD
 * reference SITE_URL so they stay in sync automatically.
 */
export const SITE_URL = "https://enicehq.com";
